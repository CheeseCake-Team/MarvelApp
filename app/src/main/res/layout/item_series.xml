<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="series"
            type="com.abaferastech.marvelapp.data.model.Series" />

        <variable
            name="listener"
            type="com.abaferastech.marvelapp.ui.home.SeriesInteractionListener" />
    </data>

    <com.google.android.material.card.MaterialCardView
        style="@style/Widget.Material3.CardView.Filled"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/space_4dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardCornerRadius="@dimen/space_4dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/image_view_story_image"
                android:layout_width="160dp"
                android:layout_height="220dp"
                app:imageUrl="@{series.thumbnail}"
                android:scaleType="fitXY"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:src="@color/material_dynamic_tertiary70" />

            <TextView
                android:id="@+id/text_view_comic_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_6dp"
                android:ellipsize="marquee"
                android:fontFamily="@font/bangers"
                android:maxLines="1"
                android:text="@{series.title}"
                app:layout_constraintEnd_toEndOf="@+id/image_view_story_image"
                app:layout_constraintHorizontal_bias="0.0"
                app:layout_constraintStart_toStartOf="@+id/image_view_story_image"
                app:layout_constraintTop_toBottomOf="@+id/image_view_story_image"
                tools:text="The amazing spider-man" />

            <TextView
                android:id="@+id/text_view_comic_issue_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_6dp"
                android:text="@{series.rating}"
                app:layout_constraintStart_toStartOf="@+id/text_view_comic_name"
                app:layout_constraintTop_toBottomOf="@+id/text_view_comic_name"
                tools:text="Issue #13" />

            <TextView
                android:id="@+id/text_view_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@{Integer.toString(Series.startYear)}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="@+id/text_view_comic_issue_number"
                app:layout_constraintTop_toBottomOf="@+id/text_view_comic_issue_number"
                tools:text="Sep, 13 2021" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>

                android:text="@{series.modified.substring(0,10)}"
                android:maxLines="1"
                android:ellipsize="marquee"
                android:fontFamily="@font/bangers"
                android:layout_marginTop="@dimen/space_6dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/text_view_comic_name"
                tools:text="Issue #13" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>
</layout>